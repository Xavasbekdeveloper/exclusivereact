import React, { memo } from "react";
import LoginComponent from "../../components/login";

const Login = () => {
  return (
    <>
      <LoginComponent />
    </>
  );
};

export default memo(Login);
